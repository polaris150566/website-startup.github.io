import * as basicgrammarfinal from './basicgrammar.mdc'

const parse = [];
parse.push( {
	topic: 'basicgrammarfinal',
	text : basicgrammarfinal.element,
	stru : basicgrammarfinal.stru
} );

export default parse;